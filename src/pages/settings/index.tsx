import React, { Component } from 'react';
import MergeCellTable from './components/MergeCellTable';

export default function () {
  return (
    <div className="Settings-page">
      {/* table with cell merge */}
      <MergeCellTable />
    </div>
  );
}
